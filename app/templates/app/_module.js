/**
 * <%= config.name.raw %> version: "0.0.1" Copyright (c) 2011-2012, <%= config.author.name %> ( <%= config.author.email %>) All Rights Reserved.
 * Available via the MIT license.
 * see: http://github.com/<%= config.github.path %> for details
 */

define( function() {

    return {
        iAm<%= config.name.camel %>: true
    }

} )